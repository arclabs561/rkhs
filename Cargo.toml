[package]
name = "rkhs"
version = "0.1.0"
authors = ["Arc <attobop@gmail.com>"]
edition = "2021"
description = "Reproducing Kernel Hilbert Space: kernels, MMD, random Fourier features"
license = "MIT OR Apache-2.0"
repository = "https://github.com/arclabs561/tekne"
keywords = ["kernel", "mmd", "rkhs", "hilbert-space", "machine-learning"]
categories = ["algorithms", "science", "mathematics"]

[dependencies]
ndarray = "0.16"
rand = "0.9"
rand_distr = "0.5"
thiserror = "2.0"

# Optional SIMD acceleration
innr = { version = "0.1.0", optional = true }

[features]
default = []
simd = ["innr"]
clam = []

[dev-dependencies]
proptest = "1.5"
criterion = { version = "0.5", features = ["html_reports"] }
faer = "0.19"

[[bench]]
name = "gram_scaling"
path = "benches/gram_scaling.rs"
harness = false

[[bench]]
name = "linalg_shootout"
path = "benches/linalg_shootout.rs"
harness = false
